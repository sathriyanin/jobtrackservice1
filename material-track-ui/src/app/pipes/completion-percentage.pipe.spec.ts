import { CompletionPercentagePipe } from './completion-percentage.pipe';

describe('CompletionPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new CompletionPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
